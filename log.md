# 变更日志

## 2026-04-14

### 页面与流程

- 收敛单入口页面结构，减少重复导出区域与重复交互入口
- 骑行控制改为在 Dashboard 内开始/结束，外层卡片仅保留设备连接与进入骑行界面
- 模拟页导出入口去重，统一通过“会话导出”卡片处理

### 物理模型与测试

- 修复风速方向计算，统一 `resolveSpeedTarget` 与 `simulateStep` 的空气力逻辑
- 补充平路/上坡/下坡多功率速度测试与风向影响测试
- 新增 `physics.md`，整理公式、参数敏感度与调参建议

### 训练模式与协议

- 训练模式升级为三种：固定阻力、固定功率（ERG）、坡度模拟（SIM）
- 新增统一 trainer command 协议与三种命令（`set-resistance` / `set-erg-power` / `set-sim-grade`）
- 控制模式在开始骑行前锁定，骑行中按锁定模式下发命令

### 导出与上传

- 增加 FIT 上传能力（上传端点配置、上传按钮、状态提示）
- 上传消息改为正式英文并附带仓库链接
- 新增 `server/` Strava 后端最小实现（OAuth、上传、状态查询）

### 文档与记录

- 新增 `project-structure.md` 与 `physics.md`
- 完成本轮关键改动记录与结构化总结
