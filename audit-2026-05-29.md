# Rider Tracker 项目审计 (2026-05-29)

## P0 — 影响稳定性/数据安全

| # | 问题 | 位置 |
|---|------|------|
| 1 | **O(n²) 内存膨胀** — 每 tick 全量拷贝 records 数组，长距离骑行 GC 卡顿严重 | `live-ride-session.js:89` |
| 2 | **同步模拟阻塞主线程** — `simulateRide` 最多 86400 次迭代的同步循环 | `simulator.js:32` |
| 3 | **Token Store 竞态条件** — 并发 OAuth 请求可能互相覆盖 token | `token-store.js:30` |
| 4 | **OAuth state Map 内存泄漏** — 放弃授权后永不清理 | `strava-routes.js:147` |
| 5 | **骑行后无总结页** — `postRideReportCard` 硬编码 `hidden=true`，停止骑行后直接跳回设置页 | `main-view.js:307` |
| 6 | **无骑行暂停功能** — 只有开始/停止，没有暂停/恢复 | `ride-service.js` |

## P1 — 代码质量 / 用户体验

| # | 问题 | 位置 |
|---|------|------|
| 7 | **重复代码** — `formatDuration`(3份)、`intensityFactor`/`variabilityIndex`/`tss`(2份)、`normalizeText`(3份) | 跨多个文件 |
| 8 | **SQLite 每次查询新建连接** — `withDatabase()` 打开→查询→关闭，无连接复用 | `activity-store.js:311` |
| 9 | **每次 state 变化全部 UI 重渲染** — 改一个 BLE 值触发 10+ 个渲染器 | `main-view.js:193` |
| 10 | **`window.alert()` 阻塞式弹窗** — API key 校验、Strava 错误都用了浏览器原生 alert | `dashboard-renderer.js`、`export-service.js` |
| 11 | **`window.prompt()/confirm()`** — 活动改名/删除用原生弹窗 | `activity-history-renderer.js` |
| 12 | **无 App 图标 / PWA manifest** — 浏览器标签无图标，无法安装为桌面应用 | `index.html` |
| 13 | **移动端适配严重不足** — `responsive.css` 仅 38 行，缺 480px/360px 断点 | `responsive.css` |
| 14 | **无键盘导航 / 焦点指示器** — 全站仅一处 `:focus-visible` 样式 | 全局 CSS |
| 15 | **FIT 导入无进度反馈** — 大文件解析卡 UI 数秒无提示 | `export-service.js:174` |
| 16 | **缺失骑行功率曲线** — 5s/1min/5min/20min 最佳功率 | `power-metrics.js` |
| 17 | **训练课程无重复/间隔结构** — 只有线性步骤 | `custom-workout-target.js` |
| 18 | **骑行中无功率区域时间分布** — 无各区域累计时间柱状图 | `live-metrics.js` |
| 19 | **无 FTP 自动检测** — 有 Ramp Test 预设但没有自动分析结果更新 FTP | `custom-workout-target.js` |
| 20 | **无路段/路线对比** — 不能对比同路段历史最佳 | route 相关 |

## P2 — 体验打磨

| # | 问题 |
|---|------|
| 21 | 无暗色模式 |
| 22 | 无路线收藏/保存功能 |
| 23 | 按钮色彩不统一（黄色 power-btn 跟绿色/蓝色体系不搭） |
| 24 | 空状态只有纯文字，无插图 |
| 25 | 视图切换无过渡动画 |
| 26 | 无骑行中音频提示（进入爬坡、功率区域变化等） |
| 27 | GPX 导入前无预览 |
| 28 | 无多周/月训练量聚合视图 |
| 29 | 图表无 y 轴单位标注，色盲用户无法区分功率/心率线 |
| 30 | 字号层级不统一（metric 40px vs hero-metric 22px） |
| 31 | 无桌面端通知（骑行结束等） |

## 测试覆盖缺口（关键模块未测试）

| 模块 | 风险 | 文件 |
|------|------|------|
| Ride Engine | 关键 | `src/app/realtime/ride-engine.js` |
| Device Service | 高 | `src/app/services/device-service.js` |
| App Store | 高 | `src/app/store/app-store.js` |
| Strava Client | 高 | `src/server/strava-client.js` |
| Export Service (完整流程) | 中 | `src/app/services/export-service.js` |
| Map Controller | 中 | `src/ui/map/map-controller.js` |
| Token Store | 中 | `src/server/token-store.js` |
| Config Store | 低 | `src/server/config-store.js` |

---

## 增量复审（2026-07-10）

### 审查范围

- 审查对象：`origin/main`
- 审查提交：`3eae309600908dc1088ca7b480b0a1562046c196`
- 验证方式：在隔离的 `origin/main` 快照中运行完整测试，并对关键场景执行定向复现
- 说明：上方内容是 2026-05-29 的历史审计快照，其中部分问题已在后续提交中修复；本节只记录本次在当前 `main` 上仍可确认的问题

### 问题汇总

| # | 优先级 | 问题 | 主要影响 | 位置 |
|---|--------|------|----------|------|
| 32 | P1 | **本地敏感 API 对任意网页开放** | 恶意网页可读取、删除本地活动，覆盖 Strava 配置，或利用已保存 token 发起 FIT 上传 | `src/server/index.js:41` |
| 33 | P1 | **多段手工路线采样使用错误坡度** | 模拟速度、坡度统计和 FTMS 坡度指令可能使用相邻路段的错误坡度 | `src/domain/route/route-builder.js:122-158` |
| 34 | P1 | **旧骑行归档回调会覆盖新骑行界面状态** | 新骑行仍在运行，但 Dashboard 被关闭并跳转到上一轮活动详情 | `src/app/services/ride-service.js:96-128` |

### 32. 本地敏感 API 缺少跨域和写操作保护

服务端通过全局 `app.use(cors())` 对所有 Origin 开放接口，同时活动写入、删除、Strava 配置和 Strava 上传接口均没有鉴权或 CSRF 防护。即使服务只监听 `127.0.0.1`，用户访问的恶意网页仍可跨域调用本机服务：先读取 `/api/activities` 获得活动 ID，再删除活动或调用 Strava 上传接口。

风险包括：

- 本地活动记录和 FIT 文件被未授权读取或删除。
- Strava Client ID / Secret 被覆盖，导致授权链路失效或被替换。
- 利用本地已保存的 OAuth token，把非预期 FIT 活动上传到用户的 Strava。
- FIT 上传使用 `multer.memoryStorage()` 且没有大小限制，跨域攻击还可进一步造成进程内存或磁盘压力。

建议：

1. 前端与 API 同源部署时直接移除全局 CORS。
2. 确需跨域时使用明确的 Origin 白名单，并拒绝无 `Origin` 或不可信 Origin 的浏览器写请求。
3. 给配置修改、活动删除和 Strava 上传等写接口增加会话鉴权及 CSRF 防护。
4. 给所有 FIT 上传入口设置文件大小限制，并在写入前校验文件类型和基本格式。

### 33. 多段手工路线的坡度采样错误

`buildRoute()` 为手工路线生成的 `points` 只包含每个路段的终点。`getRouteSampleAtDistance()` 在第一段内找到的 `nextIndex` 始终为 `0`，随后优先返回 `followingPoint.gradePercent`，因此第一段实际使用了第二段坡度。进入后续路段后，代码又会在两个终点坡度之间做线性插值，而手工分段路线应当保持每段坡度离散不变。

定向复现：

```text
路线：第一段 1 km / 2%，第二段 1 km / 8%

采样距离    实际返回    期望
0 m         8%          2%
100 m       8%          2%
999 m       8%          2%
1500 m      5%          8%
```

该采样函数同时被离线模拟、实时骑行和坡度模拟模式使用，因此错误会传播到速度计算、爬升/坡度展示以及骑行台控制目标。

建议：

- 对 `route.source === "manual"` 的路线直接使用 `getSegmentAtDistance()` 返回当前分段坡度；或重新定义手工路线点模型，明确包含距离 0 的起点和各段边界。
- 新增至少包含两个不同坡度路段的边界测试，覆盖起点、第一段内部、边界前后和第二段内部。

### 34. 异步归档结果可覆盖新骑行状态

`stopRide()` 同步结束当前骑行后启动异步 FIT 归档。归档 Promise 完成时，回调无条件读取并修改最新 store，把 `uiMode` 设置为 `activity-detail`，把 `selectedActivity` 设置为上一轮活动，并关闭 `liveRide.dashboardOpen`。

可复现时序：

1. 开始第一轮骑行。
2. 结束第一轮骑行，归档请求保持未完成。
3. 立即开始第二轮骑行，此时状态为 `isActive=true`、`dashboardOpen=true`。
4. 第一轮归档完成后，状态变为 `isActive=true`、`dashboardOpen=false`、`uiMode=activity-detail`。

结果是第二轮骑行和骑行台控制仍可能在后台运行，但用户被带到旧活动报告且看不到实时 Dashboard。

建议：

- 为每轮骑行保存稳定的 session ID 或 generation，并在归档回调中确认它仍对应最近结束的骑行。
- 如果已有新骑行处于 active 状态，只更新活动历史，不修改当前导航和 Dashboard 状态。
- 为异步回调增加取消或过期保护，避免旧请求覆盖用户之后的导航操作。

### 本次验证结果与测试缺口

- 完整测试：`179 / 179` 通过。
- 定向复现：已确认手工路线坡度返回错误。
- 定向复现：已确认旧归档 Promise 可关闭新骑行 Dashboard。
- 未修改业务代码；本节仅记录审计结论。

建议新增以下回归测试：

| 测试 | 关键断言 |
|------|----------|
| 多段手工路线采样 | 每个路段内部和边界处返回该路段的离散坡度 |
| 归档期间开始新骑行 | 旧归档完成后不得修改新骑行的 `uiMode`、`session` 和 `dashboardOpen` |
| API Origin 策略 | 非白名单 Origin 无法读取或调用活动、配置和 Strava 写接口 |
| FIT 上传大小限制 | 超限文件在进入内存或写盘前返回 `413` |
