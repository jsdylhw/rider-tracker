# Rider Tracker B 站视频稿

## 标题

Zwift 太重？我做了个能边看视频边骑车的虚拟骑行工具

## 口播稿

大家好，这期视频想聊一下我最近做的一个小项目，叫 Rider Tracker。

它是一个本地运行的虚拟骑行工具，主要是给室内骑行台用的。

最基础的功能，就是连接 FTMS 骑行台、功率计、心率带这些设备，然后在浏览器里骑。

它支持几种常见的骑行台玩法。

比如 ERG 模式。你设一个目标功率，骑行台会自动调阻力，让你尽量稳定在这个功率上。做结构化训练、甜区训练、FTP 测试，这种模式就很适合。

也支持坡度模拟。你设一个坡度，或者让路线里的坡度来控制骑行台，踩起来就会有上坡、缓坡、下坡这种变化。

还可以固定阻力。就是不搞复杂训练，想随便踩一会儿，也可以直接用一个固定阻力骑。

所以它不是只能看数据的仪表盘，而是真的可以控制骑行台。

我做这个项目，一开始其实是因为一个很简单的痛点。

在家骑车的时候，我经常想一边踩骑行台，一边看 B 站、看课程，或者看比赛。

但很多虚拟骑行软件都比较重，会占掉一个屏幕。最后就变成平板开骑行软件，电脑看视频，设备越摆越多。

Rider Tracker 里做了 PiP 小窗，就是为了解决这个问题。

骑的时候，可以把速度、功率、心率、踏频、坡度、距离、路线进度这些信息放进一个小窗里。

这个小窗可以浮在最前面，占的地方很小。

主屏幕继续看视频、看网页、看比赛，只在旁边留一点空间看骑行数据就行。

小窗里也可以显示一些图表，比如坡度、功率心率、ERG 目标功率这些。

这样骑行台控制还在，实时数据也还在，但它不会把整个电脑屏幕占满。

这是我觉得比较实用的地方。

再往前一步，这个项目真正让我觉得有意思的，是它可以做真实线路的模拟骑行。

你可以导入一条 GPX 路线，比如从 Strava、Komoot，或者其他路线工具导出的文件。

软件会解析这条路线里的距离、海拔和坡度。

骑的时候，它会根据你当前骑到路线的哪个位置，把对应的坡度发给骑行台。

也就是说，它不是一直固定阻力，也不是单纯按一个目标功率骑。

它更像是在家里骑一条具体的路。

比如你想体验环法里很有名的爬坡，像 Mont Ventoux、Alpe d'Huez 这种路线。

你可以把路线导进来，在家用骑行台去模拟这段爬坡。

坡什么时候来，什么时候缓，什么时候下坡，都会跟着路线走。

当然，这肯定不等于真实户外骑行。

风、路面、弯道、温度，这些东西室内都没法完全还原。

但至少在坡度变化、功率节奏和骑行感觉上，它会比固定阻力有意思很多。

对我来说，这就是这个项目的核心亮点：用一个比较轻量的工具，把真实路线变成室内骑行台可以骑的东西。

最后还有一个更偏实验性的功能，就是街景模拟。

既然路线本身是真实世界里的路线，那骑的时候能不能看到这条路附近的真实画面？

所以我做了一个 Street View 模式。

它会根据当前路线位置，加载附近的街景图片。

它不是视频，所以不会像真实骑行视频那样连续。

但如果隔几秒切换一次街景画面，其实已经能带来一点实景骑行的感觉。

你一边踩骑行台，一边看到路线附近的街景，会比只看地图线条更有代入感。

这个功能需要 Google API Key，所以配置上会比普通功能多一步。

但它本身不是隐藏功能，项目里已经可以用。

如果按一次完整骑行来说，流程其实很简单。

先准备路线，导入 GPX。

然后连接骑行台、功率计、心率带。

选择骑行模式。可以是 ERG，可以是固定阻力，也可以是真实路线坡度模拟。

开始骑以后，主界面会显示当前功率、心率、速度、踏频、坡度、距离和路线进度。

如果不想一直盯着主界面，就打开小窗，让它浮在视频或者网页旁边。

骑完之后，活动会保存到本地历史里。点开活动详情，可以看到这次骑行的功率区间、心率区间、能量消耗，还有功率和心率随时间变化的图表。

这些数据也可以导出成 FIT 文件。

如果配置了 Strava，也可以把 FIT 上传到 Strava。

不过这里要说清楚，我做这个功能不是为了伪造户外骑行，也不是为了刷榜。FIT 和 Strava 在这个项目里更像是数据出口：骑完以后，数据可以保存、分析，也可以同步到其他平台。

而且我不会把自己的 Strava API 密钥放进项目里。如果你想用 Strava 上传，需要创建自己的 Strava API app，然后在项目里填自己的 Client ID 和 Client Secret。

如果暂时不会配置 Strava，也没关系。可以直接把 FIT 文件下载到本地，然后自己手动上传到 Strava 或者其他支持 FIT 的平台。

也就是说，真正的使用方式还是人在骑行台上踩，设备提供真实数据，软件负责路线、阻力、显示和保存。

总结一下，Rider Tracker 不是一个大型线上骑行游戏。

它更像一个本地运行的轻量虚拟骑行工具。

它适合的人也很明确：你有骑行台，想用 ERG、坡度模拟、固定阻力这些基础功能，又不想被一个全屏软件占住电脑。

它也适合想骑真实路线的人。比如导入一段 GPX，在家里体验环法爬坡，或者骑一条自己很感兴趣的路。

如果你更喜欢折腾，还可以试试 Street View 街景模拟。

这个项目现在还在持续开发，很多地方肯定还不完美。

但它已经能跑通从设备连接、骑行台控制、真实路线模拟、小窗显示，到活动保存和数据分析的完整链路。

代码是开源的，如果你对骑行台、浏览器蓝牙、FIT 文件、虚拟骑行，或者本地训练数据分析感兴趣，也欢迎一起看代码、提 issue、提 PR，把这个项目继续完善下去。

后面我应该还会继续优化训练课程、PiP 小窗、Street View 实景骑行、小白启动方式，以及本地活动数据分析。

这期视频就先到这里。
